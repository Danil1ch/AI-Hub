/** Main shell chrome (must match TopBar layout height). */
export const TOP_BAR_HEIGHT_PX = 44

/** Inset between window chrome and the embedded webview “card”. */
export const EMBED_FRAME_INSET_PX = 3
